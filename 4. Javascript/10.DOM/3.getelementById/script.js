//----------------
//selecting elements using getelementbyid
//-------

const title = document.getElementById("conferenceTitle");
const date = document.getElementById("conferenceDate");
const Locations = document.getElementById("locationTitle");
const conferenceLocation = document.getElementById("conferenceLocation");
const speakersTitle = document.getElementById("speakersTitle");
const speakerList = document.getElementById("speakerList");

console.log({
  title,
  date,
  Locations,
  conferenceLocation,
  speakersTitle,
  speakerList,
});
